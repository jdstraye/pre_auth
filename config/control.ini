[poc]
# Flow control for POC marker behavior.
# marker_mode: optional modes (left blank for default behavior)
marker_mode=
# debug_phrase: optional lowercased substring to enable per-phrase debugging logging
# (e.g., debug_phrase=seasoned)
debug_phrase=closed re
